<h1>New feedback</h1>

<%= error_messages_for :feedback %>

<% form_for(:feedback, :url => feedback_path(:line_item_id => @line_item, :id => nil)) do |f| %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', feedback_path(:line_item_id => @line_item) %>